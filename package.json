{
  "name": "postman",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "scripts": {
    "api-tests": "newman run jsonplaceholder.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Moises Siles",
  "dependencies": {
    "newman": "^4.5.1",
    "newman-reporter-htmlextra": "^1.8.1"
  }
}
